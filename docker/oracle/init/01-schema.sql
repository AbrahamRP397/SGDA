/*
 * Esquema Oracle aislado para ejecutar las pruebas de integración de los DAO.
 * Su estructura fue obtenida de la base original en modo de solo lectura y se
 * reproduce dentro del contenedor sin modificar información de producción.
 *
 * Autor: Daniel Jared Flores Beltrán
 * Desde: 2026-08-24
 */

-- Selecciona el usuario de pruebas creado por la configuración del contenedor.
CONNECT SGDA_TEST/sgda_test_password@//localhost:1521/FREEPDB1

-- Catálogo de unidades métricas disponibles para los productos.
CREATE TABLE "METRICS" 
   (	"ID_METRIC" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"NAME" VARCHAR2(100) NOT NULL ENABLE, 
	"SHORTNAME" VARCHAR2(10) NOT NULL ENABLE, 
	"STATUS" NUMBER(1,0) DEFAULT 1 NOT NULL ENABLE, 
	 CONSTRAINT "CHK_METRICS_STATUS" CHECK (status IN (0,1)) ENABLE, 
	 PRIMARY KEY ("ID_METRIC")
  USING INDEX  ENABLE
   ) ;

CREATE TABLE "AREAS" 
   (	"ID_AREA" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"SHORTNAME" VARCHAR2(20) NOT NULL ENABLE, 
	"NAME" VARCHAR2(100) NOT NULL ENABLE, 
	"DESCRIPTION" CLOB, 
	"STATUS" NUMBER(1,0) DEFAULT 1 NOT NULL ENABLE, 
	 CONSTRAINT "CHK_AREAS_STATUS" CHECK (status IN(0,1)) ENABLE, 
	 PRIMARY KEY ("ID_AREA")
  USING INDEX  ENABLE, 
	 UNIQUE ("SHORTNAME")
  USING INDEX  ENABLE
   ) ;

CREATE TABLE "USERS" 
   (	"ID_USER" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"NAME" VARCHAR2(50) NOT NULL ENABLE, 
	"SURNAME" VARCHAR2(50) NOT NULL ENABLE, 
	"LASTNAME" VARCHAR2(50) NOT NULL ENABLE, 
	"PHONE" VARCHAR2(20), 
	"EMAIL" VARCHAR2(100) NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(255) NOT NULL ENABLE, 
	"ROLE" VARCHAR2(20) NOT NULL ENABLE, 
	"STATUS" NUMBER(1,0) DEFAULT 1 NOT NULL ENABLE, 
	"MUST_CHANGE_PASSWORD" NUMBER(1,0) DEFAULT 0 NOT NULL ENABLE, 
	"TEMPORARY_PASSWORD_EXPIRATION" TIMESTAMP (6), 
	 CONSTRAINT "CHK_USER_ROLE" CHECK (role IN ('Administrador','Almacenista')) ENABLE, 
	 CONSTRAINT "CHK_USER_STATUS" CHECK (status IN(0,1)) ENABLE, 
	 PRIMARY KEY ("ID_USER")
  USING INDEX  ENABLE, 
	 UNIQUE ("EMAIL")
  USING INDEX  ENABLE, 
	 CONSTRAINT "CHK_USERS_MUST_CHANGE_PASSWORD" CHECK (must_change_password IN (0,1)) ENABLE
   ) ;

CREATE TABLE "PROVIDERS" 
   (	"ID_PROVIDER" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"NAME" VARCHAR2(150) NOT NULL ENABLE, 
	"RFC" VARCHAR2(20) NOT NULL ENABLE, 
	"PHONE" VARCHAR2(20), 
	"EMAIL" VARCHAR2(100), 
	"CONTACTNAME" VARCHAR2(150), 
	"ADDRESS" CLOB, 
	"POSTCODE" VARCHAR2(10), 
	"SOCIALCASE" VARCHAR2(100), 
	"CONTACTPHONE" VARCHAR2(20), 
	"CONTACTEMAIL" VARCHAR2(100), 
	"STATUS" NUMBER(1,0) DEFAULT 1 NOT NULL ENABLE, 
	 CONSTRAINT "CHK_PROVIDERS_STATUS" CHECK (status IN(0,1)) ENABLE, 
	 PRIMARY KEY ("ID_PROVIDER")
  USING INDEX  ENABLE, 
	 UNIQUE ("RFC")
  USING INDEX  ENABLE
   ) ;

CREATE TABLE "PRODUCTS" 
   (	"ID_PRODUCT" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"CODE" VARCHAR2(50) NOT NULL ENABLE, 
	"NAME" VARCHAR2(150) NOT NULL ENABLE, 
	"ID_METRIC" NUMBER NOT NULL ENABLE, 
	"DESCRIPTION" CLOB, 
	"STATUS" NUMBER(1,0) DEFAULT 1 NOT NULL ENABLE, 
	 CONSTRAINT "CHK_PRODUCTS_STATUS" CHECK (status IN (0,1)) ENABLE, 
	 PRIMARY KEY ("ID_PRODUCT")
  USING INDEX  ENABLE, 
	 UNIQUE ("CODE")
  USING INDEX  ENABLE, 
	 CONSTRAINT "FK_PRODUCTS_METRIC" FOREIGN KEY ("ID_METRIC")
	  REFERENCES "METRICS" ("ID_METRIC") ENABLE
   ) ;

CREATE TABLE "PRODUCT_PROVIDERS" 
   (	"ID_PRODUCT_PROVIDER" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"ID_PRODUCT" NUMBER NOT NULL ENABLE, 
	"ID_PROVIDER" NUMBER NOT NULL ENABLE, 
	"PURCHASE_PRICE" NUMBER(12,2) DEFAULT 0 NOT NULL ENABLE, 
	"STATUS" NUMBER(1,0) DEFAULT 1 NOT NULL ENABLE, 
	 CONSTRAINT "CHK_PP_PURCHASE_PRICE" CHECK (purchase_price >= 0) ENABLE, 
	 CONSTRAINT "CHK_PP_STATUS" CHECK (status IN (0,1)) ENABLE, 
	 CONSTRAINT "PK_PRODUCT_PROVIDERS" PRIMARY KEY ("ID_PRODUCT_PROVIDER")
  USING INDEX  ENABLE, 
	 CONSTRAINT "UQ_PRODUCT_PROVIDER" UNIQUE ("ID_PRODUCT", "ID_PROVIDER")
  USING INDEX  ENABLE, 
	 CONSTRAINT "FK_PP_PRODUCT" FOREIGN KEY ("ID_PRODUCT")
	  REFERENCES "PRODUCTS" ("ID_PRODUCT") ENABLE, 
	 CONSTRAINT "FK_PP_PROVIDER" FOREIGN KEY ("ID_PROVIDER")
	  REFERENCES "PROVIDERS" ("ID_PROVIDER") ENABLE
   ) ;

CREATE TABLE "STOCK" 
   (	"ID_STOCK" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"ID_PRODUCT_PROVIDER" NUMBER NOT NULL ENABLE, 
	"QUANTITY" NUMBER(12,0) DEFAULT 0 NOT NULL ENABLE, 
	 CONSTRAINT "CHK_STOCK_QUANTITY" CHECK (quantity >= 0) ENABLE, 
	 PRIMARY KEY ("ID_STOCK")
  USING INDEX  ENABLE, 
	 CONSTRAINT "UQ_STOCK_PRODUCT_PROVIDER" UNIQUE ("ID_PRODUCT_PROVIDER")
  USING INDEX  ENABLE, 
	 CONSTRAINT "FK_STOCK_PRODUCT_PROVIDER" FOREIGN KEY ("ID_PRODUCT_PROVIDER")
	  REFERENCES "PRODUCT_PROVIDERS" ("ID_PRODUCT_PROVIDER") ENABLE
   ) ;

CREATE TABLE "ENTRIES" 
   (	"ID_ENTRY" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"CHANGE_DATE" TIMESTAMP (6) DEFAULT CURRENT_TIMESTAMP NOT NULL ENABLE, 
	"INVOICE_NUMBER" VARCHAR2(50) NOT NULL ENABLE, 
	"FOLIO_NUMBER" VARCHAR2(20) NOT NULL ENABLE, 
	"ID_USER" NUMBER NOT NULL ENABLE, 
	"ID_PROVIDER" NUMBER NOT NULL ENABLE, 
	"TOTAL_ALL_PRICES" NUMBER(14,2) DEFAULT 0 NOT NULL ENABLE, 
	 CONSTRAINT "CHK_ENTRIES_TOTAL" CHECK (total_all_prices >= 0) ENABLE, 
	 CONSTRAINT "CHK_ENTRIES_FOLIO" CHECK (REGEXP_LIKE(folio_number, '^E[0-9]{8}$')) ENABLE, 
	 PRIMARY KEY ("ID_ENTRY")
  USING INDEX  ENABLE, 
	 CONSTRAINT "UQ_ENTRIES_FOLIO" UNIQUE ("FOLIO_NUMBER")
  USING INDEX  ENABLE, 
	 CONSTRAINT "FK_ENTRIES_USER" FOREIGN KEY ("ID_USER")
	  REFERENCES "USERS" ("ID_USER") ENABLE, 
	 CONSTRAINT "FK_ENTRIES_PROVIDER" FOREIGN KEY ("ID_PROVIDER")
	  REFERENCES "PROVIDERS" ("ID_PROVIDER") ENABLE
   ) ;

CREATE TABLE "ENTRY_PRODUCTS" 
   (	"ID_ENTRY_PRODUCT" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"ID_ENTRY" NUMBER NOT NULL ENABLE, 
	"ID_PRODUCT_PROVIDER" NUMBER NOT NULL ENABLE, 
	"QUANTITY" NUMBER(12,0) NOT NULL ENABLE, 
	"REMAINING_QUANTITY" NUMBER(12,0) NOT NULL ENABLE, 
	"UNIT_PRICE" NUMBER(12,2) NOT NULL ENABLE, 
	"TOTAL_PRICE" NUMBER(14,2) GENERATED ALWAYS AS ("QUANTITY"*"UNIT_PRICE") VIRTUAL , 
	 CONSTRAINT "CHK_EP_QUANTITY" CHECK (quantity > 0) ENABLE, 
	 CONSTRAINT "CHK_EP_REMAINING" CHECK (
            remaining_quantity >= 0
            AND remaining_quantity <= quantity
        ) ENABLE, 
	 CONSTRAINT "CHK_EP_UNIT_PRICE" CHECK (unit_price >= 0) ENABLE, 
	 PRIMARY KEY ("ID_ENTRY_PRODUCT")
  USING INDEX  ENABLE, 
	 CONSTRAINT "UQ_ENTRY_PRODUCT_PROVIDER" UNIQUE ("ID_ENTRY", "ID_PRODUCT_PROVIDER")
  USING INDEX  ENABLE, 
	 CONSTRAINT "FK_EP_ENTRY" FOREIGN KEY ("ID_ENTRY")
	  REFERENCES "ENTRIES" ("ID_ENTRY") ON DELETE CASCADE ENABLE, 
	 CONSTRAINT "FK_EP_PRODUCT_PROVIDER" FOREIGN KEY ("ID_PRODUCT_PROVIDER")
	  REFERENCES "PRODUCT_PROVIDERS" ("ID_PRODUCT_PROVIDER") ENABLE
   ) ;

CREATE TABLE "EXITS" 
   (	"ID_EXIT" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"CHANGE_DATE" TIMESTAMP (6) DEFAULT CURRENT_TIMESTAMP NOT NULL ENABLE, 
	"INVOICE_NUMBER" VARCHAR2(50) NOT NULL ENABLE, 
	"FOLIO_NUMBER" VARCHAR2(20) NOT NULL ENABLE, 
	"ID_USER" NUMBER NOT NULL ENABLE, 
	"ID_AREA" NUMBER NOT NULL ENABLE, 
	"BUYER_NAME" VARCHAR2(150) NOT NULL ENABLE, 
	"TOTAL_ALL_PRICES" NUMBER(14,2) DEFAULT 0 NOT NULL ENABLE, 
	 CONSTRAINT "CHK_EXITS_TOTAL" CHECK (total_all_prices >= 0) ENABLE, 
	 CONSTRAINT "CHK_EXITS_FOLIO" CHECK (REGEXP_LIKE(folio_number, '^S[0-9]{8}$')) ENABLE, 
	 PRIMARY KEY ("ID_EXIT")
  USING INDEX  ENABLE, 
	 CONSTRAINT "UQ_EXITS_FOLIO" UNIQUE ("FOLIO_NUMBER")
  USING INDEX  ENABLE, 
	 CONSTRAINT "FK_EXITS_USER" FOREIGN KEY ("ID_USER")
	  REFERENCES "USERS" ("ID_USER") ENABLE, 
	 CONSTRAINT "FK_EXITS_AREA" FOREIGN KEY ("ID_AREA")
	  REFERENCES "AREAS" ("ID_AREA") ENABLE
   ) ;

CREATE TABLE "EXIT_PRODUCTS" 
   (	"ID_EXIT_PRODUCT" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"ID_EXIT" NUMBER NOT NULL ENABLE, 
	"ID_PRODUCT" NUMBER NOT NULL ENABLE, 
	"QUANTITY" NUMBER(12,0) NOT NULL ENABLE, 
	"UNIT_PRICE" NUMBER(14,4) NOT NULL ENABLE, 
	"TOTAL_PRICE" NUMBER(14,2) NOT NULL ENABLE, 
	 CONSTRAINT "CHK_EXIT_PRODUCTS_QUANTITY" CHECK (quantity > 0) ENABLE, 
	 CONSTRAINT "CHK_EXIT_PRODUCTS_UNIT_PRICE" CHECK (unit_price >= 0) ENABLE, 
	 CONSTRAINT "CHK_EXIT_PRODUCTS_TOTAL" CHECK (total_price >= 0) ENABLE, 
	 PRIMARY KEY ("ID_EXIT_PRODUCT")
  USING INDEX  ENABLE, 
	 CONSTRAINT "UQ_EXIT_PRODUCT" UNIQUE ("ID_EXIT", "ID_PRODUCT")
  USING INDEX  ENABLE, 
	 CONSTRAINT "FK_EXIT_PRODUCTS_EXIT" FOREIGN KEY ("ID_EXIT")
	  REFERENCES "EXITS" ("ID_EXIT") ON DELETE CASCADE ENABLE, 
	 CONSTRAINT "FK_EXIT_PRODUCTS_PRODUCT" FOREIGN KEY ("ID_PRODUCT")
	  REFERENCES "PRODUCTS" ("ID_PRODUCT") ENABLE
   ) ;

CREATE TABLE "EXIT_ALLOCATIONS" 
   (	"ID_EXIT_ALLOCATION" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"ID_EXIT_PRODUCT" NUMBER NOT NULL ENABLE, 
	"ID_ENTRY_PRODUCT" NUMBER NOT NULL ENABLE, 
	"QUANTITY" NUMBER(12,0) NOT NULL ENABLE, 
	"UNIT_COST" NUMBER(12,2) NOT NULL ENABLE, 
	"TOTAL_COST" NUMBER(14,2) GENERATED ALWAYS AS ("QUANTITY"*"UNIT_COST") VIRTUAL , 
	 CONSTRAINT "CHK_EXIT_ALLOCATION_QUANTITY" CHECK (quantity > 0) ENABLE, 
	 CONSTRAINT "CHK_EXIT_ALLOCATION_UNIT_COST" CHECK (unit_cost >= 0) ENABLE, 
	 PRIMARY KEY ("ID_EXIT_ALLOCATION")
  USING INDEX  ENABLE, 
	 CONSTRAINT "UQ_EXIT_ALLOCATION_LOT" UNIQUE ("ID_EXIT_PRODUCT", "ID_ENTRY_PRODUCT")
  USING INDEX  ENABLE, 
	 CONSTRAINT "FK_EXIT_ALLOC_ENTRY_PRODUCT" FOREIGN KEY ("ID_ENTRY_PRODUCT")
	  REFERENCES "ENTRY_PRODUCTS" ("ID_ENTRY_PRODUCT") ENABLE
   ) ;

CREATE TABLE "PASSWORD_RESET_TOKENS" 
   (	"ID_TOKEN" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"ID_USER" NUMBER NOT NULL ENABLE, 
	"TOKEN" VARCHAR2(255) NOT NULL ENABLE, 
	"EXPIRATION" TIMESTAMP (6) NOT NULL ENABLE, 
	"USED" NUMBER(1,0) DEFAULT 0 NOT NULL ENABLE, 
	"CREATED_AT" TIMESTAMP (6) DEFAULT CURRENT_TIMESTAMP, 
	 CONSTRAINT "CHK_TOKEN_USED" CHECK (used IN(0,1)) ENABLE, 
	 PRIMARY KEY ("ID_TOKEN")
  USING INDEX  ENABLE, 
	 UNIQUE ("TOKEN")
  USING INDEX  ENABLE, 
	 CONSTRAINT "FK_TOKEN_USER" FOREIGN KEY ("ID_USER")
	  REFERENCES "USERS" ("ID_USER") ON DELETE CASCADE ENABLE
   ) ;

CREATE INDEX "IDX_EXIT_ALLOC_ENTRY_PRODUCT" ON "EXIT_ALLOCATIONS" ("ID_ENTRY_PRODUCT") 
  ;

CREATE INDEX "IDX_EXITS_AREA" ON "EXITS" ("ID_AREA") 
  ;

CREATE INDEX "IDX_EXITS_CHANGE_DATE" ON "EXITS" ("CHANGE_DATE") 
  ;

CREATE INDEX "IDX_EXITS_USER" ON "EXITS" ("ID_USER") 
  ;

CREATE INDEX "IDX_PP_PRODUCT" ON "PRODUCT_PROVIDERS" ("ID_PRODUCT") 
  ;

CREATE INDEX "IDX_PP_PROVIDER" ON "PRODUCT_PROVIDERS" ("ID_PROVIDER") 
  ;

